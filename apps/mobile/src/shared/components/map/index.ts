/**
 * Map components (CourseMap, MapMarker, SplitMarker, MarkerCluster).
 *
 * react-native-maps is installed and configured, but is intentionally not
 * imported here yet: it pulls a native module that is only integrated in
 * Milestone 7 (Course Maps). See docs/COMPONENT_LIBRARY.md.
 */
export {};
