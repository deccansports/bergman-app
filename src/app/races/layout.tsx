// src/app/races/layout.tsx
import type { ReactNode } from 'react';
import React from 'react';

export default function RacesLayout({ children }: { children: ReactNode }) {
  return (
    <>
      {children}
    </>
  );
}
